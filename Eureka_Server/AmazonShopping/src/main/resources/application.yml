eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 9999
spring:
  application:
    name: SHOPPING-SERVICE
    
    
#Runs on port 9999

#Has a controller that uses RestTemplate to call PAYMENT-SERVICE